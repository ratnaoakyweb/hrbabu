<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    android:background="#F5F5F5">

    <!-- Header -->
    <LinearLayout
        android:id="@+id/headerLayout"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:background="@color/colorPrimary"
        android:orientation="horizontal"
        android:padding="12dp"
        android:gravity="center_vertical"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <ImageView
            android:id="@+id/btnBack"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@drawable/ic_back"
            android:contentDescription="Back"
            app:tint="@color/white" />

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:gravity="center"
            android:text="Employee Profile"
            android:textColor="@color/white"
            android:textStyle="bold"
            android:textSize="16sp" />
    </LinearLayout>

    <!-- Scrollable Content -->
    <ScrollView
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:padding="12dp"
        app:layout_constraintTop_toBottomOf="@id/headerLayout"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:background="@android:color/white"
            android:padding="16dp"
            android:layout_marginBottom="12dp">

            <!-- Section: Personal Information -->
            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Personal Information"
                android:textStyle="bold"
                android:textSize="16sp"
                android:textColor="@color/colorPrimary"
                android:layout_marginBottom="8dp" />

            <View
                android:layout_width="match_parent"
                android:layout_height="1dp"
                android:background="#DDDDDD"
                android:layout_marginBottom="12dp" />

            <!-- Info Row -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:paddingBottom="8dp">

                <TextView android:text="Full Name" style="@style/ProfileLabel" />
                <TextView android:id="@+id/tvFullName" style="@style/ProfileValue" />

                <TextView android:text="Email" style="@style/ProfileLabel" />
                <TextView android:id="@+id/tvEmail" style="@style/ProfileValue" />

                <TextView android:text="Phone" style="@style/ProfileLabel" />
                <TextView android:id="@+id/tvPhone" style="@style/ProfileValue" />

                <TextView android:text="Date of Birth" style="@style/ProfileLabel" />
                <TextView android:id="@+id/tvDob" style="@style/ProfileValue" />

                <TextView android:text="Joining Date" style="@style/ProfileLabel" />
                <TextView android:id="@+id/tvJoiningDate" style="@style/ProfileValue" />
            </LinearLayout>

            <!-- Section: Company Info -->
            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Company Information"
                android:textStyle="bold"
                android:textSize="16sp"
                android:textColor="@color/colorPrimary"
                android:layout_marginTop="16dp"
                android:layout_marginBottom="8dp" />

            <View
                android:layout_width="match_parent"
                android:layout_height="1dp"
                android:background="#DDDDDD"
                android:layout_marginBottom="12dp" />

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical">

                <TextView android:text="Company Name" style="@style/ProfileLabel" />
                <TextView android:id="@+id/tvCompanyName" style="@style/ProfileValue" />

                <TextView android:text="Department" style="@style/ProfileLabel" />
                <TextView android:id="@+id/tvDepartment" style="@style/ProfileValue" />

                <TextView android:text="Designation" style="@style/ProfileLabel" />
                <TextView android:id="@+id/tvDesignation" style="@style/ProfileValue" />

                <TextView android:text="Location" style="@style/ProfileLabel" />
                <TextView android:id="@+id/tvLocation" style="@style/ProfileValue" />

                <TextView android:text="Manager Name" style="@style/ProfileLabel" />
                <TextView android:id="@+id/tvManager" style="@style/ProfileValue" />
            </LinearLayout>

            <!-- Section: Shift Info -->
            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Shift Information"
                android:textStyle="bold"
                android:textSize="16sp"
                android:textColor="@color/colorPrimary"
                android:layout_marginTop="16dp"
                android:layout_marginBottom="8dp" />

            <View
                android:layout_width="match_parent"
                android:layout_height="1dp"
                android:background="#DDDDDD"
                android:layout_marginBottom="12dp" />

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical">

                <TextView android:text="Shift Name" style="@style/ProfileLabel" />
                <TextView android:id="@+id/tvShiftName" style="@style/ProfileValue" />

                <TextView android:text="Shift Code" style="@style/ProfileLabel" />
                <TextView android:id="@+id/tvShiftCode" style="@style/ProfileValue" />

                <TextView android:text="Biometric ID" style="@style/ProfileLabel" />
                <TextView android:id="@+id/tvBiometricId" style="@style/ProfileValue" />
            </LinearLayout>

        </LinearLayout>
    </ScrollView>

</androidx.constraintlayout.widget.ConstraintLayout>
