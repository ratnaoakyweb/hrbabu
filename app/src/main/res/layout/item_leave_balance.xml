<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@android:color/white"
    android:padding="16dp">

    <!-- Leave Type Name -->
    <TextView
        android:id="@+id/tvLeaveTypeName"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:textColor="#212121"
        android:textStyle="bold"
        android:textSize="16sp"
        android:ellipsize="end"
        android:singleLine="true"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/tvNoOfLeaves"
        tools:text="Casual Leave" />

    <!-- Number of Leaves -->
    <TextView
        android:id="@+id/tvNoOfLeaves"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="#1A73E8"
        android:textStyle="bold"
        android:textSize="18sp"
        android:layout_marginEnd="4dp"
        app:layout_constraintEnd_toStartOf="@+id/tvLevelRemaining"
        app:layout_constraintBaseline_toBaselineOf="@+id/tvLeaveTypeName"
        tools:text="5" />

    <!-- "Days" label -->
    <TextView
        android:id="@+id/tvLevelRemaining"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="days"
        android:textColor="#757575"
        android:textSize="14sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBaseline_toBaselineOf="@+id/tvNoOfLeaves"
        tools:text="days" />

    <!-- Consumed Info -->
    <TextView
        android:id="@+id/tvLeaveConsumed"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:textColor="#757575"
        android:textSize="13sp"
        app:layout_constraintTop_toBottomOf="@+id/tvLeaveTypeName"
        app:layout_constraintStart_toStartOf="@+id/tvLeaveTypeName"
        tools:text="2 days consumed" />

    <!-- Divider Line -->
    <View
        android:id="@+id/divider"
        android:layout_width="0dp"
        android:layout_height="1dp"
        android:background="#E0E0E0"
        android:layout_marginTop="8dp"
        app:layout_constraintTop_toBottomOf="@id/tvLeaveConsumed"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
